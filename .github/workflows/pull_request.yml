name: Pull request

on:
  pull_request:


jobs:
  test:
    uses: ./.github/workflows/android_build.yml
    with:
      branch: ${{github.head_ref}}
      gradlew-command: assembleDevelop --no-daemon --profile --stacktrace --debug
      build-debug-tests: true
    secrets: inherit
