name: Pull request

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2

      - name: Set Environment Variables
        uses: ./.github/workflows/setvars/
        with:
          varFilePath: ${{ secrets }}

      - name: 🔧 Install dependencies
        uses: ./.github/workflows/install/

      - name: 🧪 Run tests
        run: ./gradlew runTest
