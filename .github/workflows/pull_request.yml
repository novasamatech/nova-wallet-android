name: Pull request

on:
  pull_request:

jobs:
  unit-tests:
    uses: nova-wallet/nova-wallet-android/.github/workflows/android_build.yml@${{ github.head_ref || github.ref_name }}
    secrets: inherit
