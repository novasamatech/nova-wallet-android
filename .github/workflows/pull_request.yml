name: Pull request

on:
  pull_request:


jobs:
  test:
    uses: novasamatech/nova-wallet-android/.github/workflows/android_build.yml@fix/build_with_rust
    with:
      branch: ${{github.head_ref}}
    secrets: inherit
