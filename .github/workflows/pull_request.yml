name: Pull request

on:
  pull_request:

jobs:
  call-workflow-passing-data:
    uses: nova-wallet/nova-wallet-android/.github/workflows/android_build.yml@fix/github_actions
    with:
      username: mona
    secrets: inherit
