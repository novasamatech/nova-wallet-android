// Nothing about delivery is configured per build here: every build collects (subject to the user's
// consent) and attests with Play Integrity, and the host comes from the global remote config (infraUrl).
android {

    buildFeatures {
        buildConfig = true
    }

    namespace 'io.novafoundation.nova.analytics'
}

dependencies {
    implementation project(':common')
    implementation project(':infrastructure')
    implementation project(':core-db')

    implementation kotlinDep
    implementation coroutinesDep
    implementation lifecycleProcessDep

    implementation daggerDep
    ksp daggerCompiler

    implementation retrofitDep
    implementation gsonDep
    implementation roomDep

    testImplementation jUnitDep
    testImplementation coroutinesTestDep
}
